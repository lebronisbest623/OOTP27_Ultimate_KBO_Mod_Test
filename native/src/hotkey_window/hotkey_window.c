#include "hotkey_window.h"

/*
 * The F2 hub implementation lives in hotkey_window_runtime.c and focused
 * ui/state helper modules. Keep this translation unit as the public entry
 * file so native/src/hotkey_window/hotkey_window.c stays intentionally thin.
 */
